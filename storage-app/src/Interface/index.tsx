// TODO Would be good to inform myself with this thingy
// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface Button {
    btnClass: string;
    text: string;
    onClick: () => void;
}

// TODO Just to not lose it
// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface GithubAuth {
    clientId: string;
    clientSecret: string;
}